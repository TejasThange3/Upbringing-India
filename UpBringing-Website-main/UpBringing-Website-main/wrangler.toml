# Wrangler configuration for Cloudflare Pages
# This file is optional - you can also configure everything in the Cloudflare dashboard

name = "upbringing-app"
compatibility_date = "2024-01-01"

# Pages configuration
pages_build_output_dir = "build"





# Environment variables for preview deployments
# For production, set these in Cloudflare dashboard
[env.preview.vars]
# VITE_API_ENDPOINT will be set in Cloudflare dashboard
